# Production build — same-origin API on AWS EC2 (Apache/nginx → backend/public)
VITE_API_BASE_URL=/api/v1
VITE_BASE_PATH=/

# Separate API host (S3 frontend + ALB API):
# VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
