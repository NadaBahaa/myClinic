# Dev: leave empty so the Vite dev server proxies /api/* → Laravel (php artisan serve on 8001).
# For XAMPP only, set e.g. VITE_API_BASE_URL=http://localhost/Beauty%20Clinic%20Management%20App%20(Final-2)/backend/public/api/v1
VITE_API_BASE_URL=
